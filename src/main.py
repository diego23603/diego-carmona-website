if __name__ == "__main__":
    print("Script principal iniciado")
    # Llamar a la función principal del extractor de rutas
    from extractor import extract_paths_from_mongo
    extract_paths_from_mongo()
